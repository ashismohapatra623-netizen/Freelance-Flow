{
  "name": "freelancer-manager",
  "version": "1.0.0",
  "description": "A single-user freelancer management tool to track clients, projects, tasks, and billable hours.",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev:client": "cd client && npm run dev",
    "test": "cd server && python -m pytest ../tests/ -v && cd ../client && npx vitest run",
    "test:server": "cd server && python -m pytest ../tests/ -v",
    "test:client": "cd client && npx vitest run",
    "seed": "cd server && python -m seeds.dev_seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
